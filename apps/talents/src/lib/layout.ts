// Astra reference `.wrap` container: max-width 1320px, with side margins of
// 56px / 32px / 20px at wide / medium / narrow viewports. Expressed as
// Tailwind utilities (rather than a bespoke global CSS class) to match how
// every other production page is built.
export const WRAP = "mx-auto max-w-[1320px] px-5 sm:px-8 lg:px-14";
