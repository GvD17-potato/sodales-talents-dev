# Public-preview data mode. Use "fixture" only for an explicitly labelled
# testing milestone; real database implementation must use "database".
TALENTS_DATA_SOURCE="fixture"

# Neon Postgres (server-only; never expose these as NEXT_PUBLIC_* values)
DATABASE_URL=""
DATABASE_URL_UNPOOLED=""
NEON_BRANCH=""

# Neon Auth
NEON_AUTH_BASE_URL=""
NEON_AUTH_JWKS_URL=""
NEON_AUTH_COOKIE_SECRET=""
ADMIN_EMAIL=""
ADMIN_PASSWORD=""
